{
  "name": "url-shortener-memo",
  "version": "0.0.1",
  "description": "URL shortener with message page",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rpedsel",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.14.1",
    "express": "^4.13.3",
    "mongoose": "^4.2.9",
    "nodemon": "^1.17.5",
    "redis": "^2.8.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
